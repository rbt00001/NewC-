int main() {
    const double pi = 3.14;
     // pi = 0;
     // Placing const in front of the pi, it prevents the number from 
     // being altered and will throw an error if it is used. 
    return 0;
}